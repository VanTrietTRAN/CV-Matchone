# ─── AI Service Requirements ──────────────────────────────────────────────────
# Phiên bản hiện tại: Sử dụng Gemini API (nhẹ, ~60MB RAM)
# Phù hợp với Render Free tier (512MB RAM limit)
#
# ─── [LEGACY - Local model dependencies] ─────────────────────────────────────
# Uncomment các dòng dưới đây nếu muốn rollback về local SentenceTransformer
# (Yêu cầu ít nhất 1.5GB RAM — cần Render Starter trở lên)
#
# torch==2.11.0
# sentence-transformers==5.4.1
# transformers==5.5.4
# tokenizers==0.22.2
# huggingface_hub==1.11.0
# safetensors==0.7.0
# hf-xet==1.4.3
# filelock==3.28.0
# fsspec==2026.3.0
# ─────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi==0.136.0
uvicorn==0.44.0
starlette==1.0.0

# Data validation
pydantic==2.13.1
pydantic_core==2.46.1

# Gemini AI API (thay thế local model)
# SDK mới. `google-generativeai` cũ đã ngừng hỗ trợ — đừng quay lại.
google-genai>=1.0.0

# Math (dùng cho cosine similarity trong matching_service)
numpy==2.4.4
scikit-learn==1.8.0
scipy==1.17.1

# Utilities
python-dotenv>=1.0.0
annotated-types==0.7.0
anyio==4.13.0
httpx==0.28.1
